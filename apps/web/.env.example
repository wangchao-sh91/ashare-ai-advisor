# Browser-visible values only. Never put provider credentials in VITE_* variables.
# Keep the API base URL same-origin; Vite proxies /api to the local API in development.
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
VITE_DEV_HOST=127.0.0.1
VITE_DEV_PORT=5173
