# v1.1 Provider 迁移验证记录

验证日期：2026-08-10；环境：Linux、CPython 3.12.13、Node.js 22、Docker Compose v2。
所有真实 Provider 输出均经过脱敏，只记录计数和状态。

## 离线质量套件

以下命令均在仓库根目录执行（括号内为结果）：

```bash
cd apps/api && .venv/bin/python -m pytest
# 122 passed；总覆盖率 85%

cd apps/api && .venv/bin/python -m ruff check .
# All checks passed

cd apps/api && .venv/bin/python -m ruff format --check .
# 65 files already formatted

cd apps/api && .venv/bin/python -m mypy app tests
# Success: no issues found in 60 source files

cd apps/api && .venv/bin/python scripts/export_contracts.py
cd apps/api && .venv/bin/python -m pytest tests/api/test_contract_exports.py tests/api/test_chat_stream.py tests/core/test_logging.py -q
# 14 passed（契约、SSE 兼容和日志脱敏专项）

cd apps/api && .venv/bin/pre-commit run detect-secrets --all-files
# detect secrets: Passed

cd apps/web && npm run lint && npm run format:check && npm run typecheck
# ESLint、Prettier、TypeScript 均通过

cd apps/web && npm test && npm run build
# 2 个测试文件、10 个测试通过；Vite 生产构建成功

bash scripts/compose-integration.sh
# PASS compose integration and MVP acceptance scenarios
```

Compose 验收使用 `PROVIDER_MODE=fake` 和临时环境文件，无真实密钥。它从
`apps/api/requirements.lock` 无缓存构建镜像，验证 `/health`、带 Provider 状态的
`/ready`、Web 反向代理、既有 SSE 事件、典型/越界问答、API 端口隔离、服务重启和优雅
停止。首次运行发现旧脚本错误要求快速完成的 SSE 进程仍存活；删除时长假设、保留完整
事件断言后重跑通过。

## 真实 Provider 与降级

```bash
cd apps/api && LIVE_PROVIDER_SMOKE=1 .venv/bin/python scripts/tushare_daily_smoke.py
# PASS Tushare: received 5 validated daily record(s)

cd apps/api && LIVE_PROVIDER_SMOKE=1 .venv/bin/python scripts/doubao_search_smoke.py
# PASS Doubao Search: received 1 normalized result(s)

cd apps/api && .venv/bin/python -m pytest tests/agent/test_orchestrator.py tests/api/test_health.py -q
# 12 passed
```

Tushare 检查只请求 `600519.SH` 的 2026-08-03 至 2026-08-07 日线，并通过字段、日期和
单位转换验证。豆包检查只请求一条宽基指数 Web 结果，并要求至少一条规范化、可引用证据；
空结果不会再误报成功。编排/就绪专项覆盖价格失败但搜索成功、搜索额度失败但价格成功、
全部类别失败、完整/部分回答及 Provider readiness，确认不会跨类别替代证据。

监控指标、配额/缓存策略、发布步骤及上一镜像/配置成组回滚流程见
[`deployment.md`](deployment.md)。
